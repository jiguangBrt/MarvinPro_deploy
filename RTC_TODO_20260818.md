# RTC TODO - 2026-08-18

目标：记录 2026-08-18 的 RTC 决策和后续实现项。soft mask 不再修改；当前唯一计划中的行为变更是
synchronized fallback 稳定一次后，按故障类别有限地重新进入 RTC。

## 2026-08-19 protocol v10 实现状态

- [x] H20 synchronized/tracking/fallback 已统一为 bridge-owned timed chunk：名义 4 秒，默认增加 1 秒 grace。
- [x] 健康 timeout 由 controller 原子锁存最新实测双臂位置，保留最后夹爪命令，稳定后从新图像重推理；
  连续两次 timeout 后固定 hold 并结束。
- [x] 新计划优先使用 3-knot quintic C2 handoff，失败再试 2-knot；动力学或安全限制失败时原子拒绝。
- [x] RTC failure 使用结构化 reason code。late/discard、transport timeout、observation lag 和 C2 merge
  infeasible 可恢复；clipping、freeze、stale、heartbeat/timer、状态门、事务/协议/shape/finite/URDF 错误不可恢复。
- [x] 可恢复故障会废弃旧连接/request/epoch，实测 hold，执行至少一个 clean synchronized chunk，再用新 H20
  普通推理初始化新 DelayEstimator epoch 和 `s=10` RTC bootstrap；每个 episode 最多 3 次。
- [x] 本地 OpenPI WebSocket client 已支持 connect/request timeout、`close()` 解阻塞和 metadata 校验重连；
  MarvinPro 默认连接 5 秒、请求 2 秒。
- [x] 无真机验证完成：deploy `100 passed`、OpenPI client `7 passed`、两端 Ruff 通过；远程 H20 smoke、
  20 次持久 RTC 请求、强制 recv timeout 后 reconnect 均通过。
- [ ] 尚需 protocol v10 motion-disabled dry-run 和三步真机验收；禁止碰桌或人为阻挡制造 timeout。

## 修改前基线核对

- [x] 本地待部署基线为 `H=20`、`s=10`、`H-s=10`、`d_max=4`；不再按旧文档中的 `H=10`、`s=6`
  实施。
- [x] 远程 GPU 1 的 `192.168.50.73:8000` 已运行 H20 checkpoint
  `pi05_marvinpro_red_cones_h20/.../79999`；2026-08-18 已完成不连接真机的 WebSocket 验证。
- [x] policy metadata 已确认公布 `rtc_v1`、H20/s10、native/model action dim 16/32、`d_max=4` 和
  `prefix_attention_schedule=exp`，与当前客户端基线一致。
- [ ] 真机前仍需确认 deploy、controller bridge 和测试命令处于同一 protocol v10 commit，并核对远程 policy
  metadata；
  今天的 policy-only 测试不能替代 controller dry-run。
- [ ] 保留当前 hard anchor、2～3 knot C1/C2 blend、URDF、tracking、clipping、stale、heartbeat、事务 ID
  和 `d_actual` 物理计数保护，不用本次优化绕过这些安全门。

## 1. 指数 soft mask 基线冻结（已决）

### 最终决策

保留 OpenPI 当前的 `schedule=exp`，本轮不修改 soft-mask 公式、`d_max` 或远程 policy metadata。

```text
P = H - s
start = min(d_pred, P)
base[i] = clip(1 + (start - 1 - i) / (P - start + 1), 0, 1)
weight[i] = base[i] * (exp(base[i]) - 1) / (e - 1), i < P
weight[i] = 0,                                         i >= P
```

理由：soft-mask 非零范围由 `P=H-s` 决定，不由 `d_max` 决定。H20/s10 在常见 `d_pred=2` 时已有 8 个 soft
transition knot；增大 `d_max` 不会改变同一个 `d_pred` 的权重表，实际使用更大的 `d_pred` 反而会减少
`P-d_pred`，缩短 soft transition。`d_max` 只按稳定延迟预算设置，不作为平滑性调参。

当前约束：

- [x] 本地客户端继续发送 `schedule=exp` 和 `beta=5.0`。
- [x] 远程 policy metadata 继续公布 `prefix_attention_schedule=exp`，并结构化拒绝不支持的 schedule。
- [x] 暂不修改 `src/openpi/models/pi0.py` 的 `get_rtc_prefix_weights()`。
- [x] 保持 `d_max=4`；不得为了获得更多平滑点而提高 `d_max`，也不得设置 `d_max=H-s=10`。
- [x] 本 TODO 不再安排 soft-mask schedule 变更或 A/B；后续问题先从真实 merge 数据、C2 边界和任务初始条件排查。

### 2026-08-18 非真机验证

- [x] `scripts/marvinpro_rtc_smoke.py` 通过：普通推理、RTC 推理均返回有限 `(20,16)`，错误 prefix 被拒绝后
  连接仍可继续使用。
- [x] 同一持久连接执行 2 次 discarded warmup 和 20 次有效 RTC 请求，`d_pred=1..4` 各 5 次，ID、shape 和
  finite 全部通过。
- [x] wall latency：p50 `375.670 ms`、p95 `447.539 ms`、max `568.937 ms`；5 Hz 加 50 ms guard 后，
  p95 对应 `d_pred=3`，仍在 `d_max=4` 内。
- [x] server infer p50/p95/max 为 `210.021/221.055/222.916 ms`；network residual estimate
  p50/p95/max 为 `163.474/231.425/351.140 ms`。
- [x] 错误 prefix、`d_pred=5`、错误 `s` 和不支持的 schedule 均返回 `invalid_rtc_request`；随后有效请求成功。
- [x] deploy 全套 `80 passed`；OpenPI 定向 CPU `25 passed, 2 deselected`；两端相关 Ruff 通过。
- [ ] 上述结果不包含真机、controller bridge、真实 observation、tracking、C2 blend 或 fallback 恢复验证。

## 2. synchronized fallback 稳定一次后重新进入 RTC

### 目标状态机

```text
RTC 可恢复异常
-> invalidate 当前 RTC request/epoch
-> fixed hold
-> tracking 到位并连续稳定 0.20 s
-> 获取 hold 之后的新 state/image
-> 执行 1 个完整 synchronized H20 chunk
-> 在 chunk 末端再次 tracking 到位并连续稳定 0.20 s
-> 获取末端稳定之后的新 state/image
-> 建立新的 RTC delay/request/timeline epoch
-> 从新观测推理并加载新的 RTC 初始 chunk
-> 回到 continuous RTC
```

这里的“稳定一次”定义为：完整 synchronized chunk 成功执行到末端 checkpoint，并满足 tracking tolerance、
source timestamp 连续前进、settle 0.20 s、无 clipping/hard freeze/stale/timer overrun，再取得满足时间屏障的
新观测。不能仅靠等待固定秒数判定稳定。

### 故障分类

下列故障允许自动恢复：

- [x] RTC late-result discard / delay budget miss。
- [x] 明确的瞬时 transport timeout 或 policy server 暂时不可用。
- [x] 可通过重新观测消除的 observation freshness/lag。
- [x] 单次 RTC C2 merge 不可行；原 merge 仍原子拒绝且不放宽 jerk，只有经过 clean chunk 换边界后才重试。

下列故障继续锁存，不自动重进 RTC：

- [x] arm clipping、tracking hard freeze、joint stale、timer overrun、heartbeat、Input Mode/Robot Ready/arm state
  异常。
- [x] session/plan/timeline/checkpoint/request ID 不匹配。
- [x] 非 finite、shape、URDF、安全包络、协议版本或本地 invariant 错误。

bootstrap inference/安全 C2 handoff 失败会消耗一次 recovery attempt；若属于瞬时 transport/C2 边界问题，
允许在剩余次数内重新 hold、clean sync 和 bootstrap，但不会放宽动力学上限。到达 3 次上限后固定 hold。

不要继续用一个宽泛的 `except Exception` 同时决定“可恢复”和“不可恢复”；先形成带 reason code 的明确分类。

### 实现约束

- [x] 将当前运行到 episode 结束的 synchronized fallback 拆出“只执行一个恢复 chunk”的入口，并返回末端
  checkpoint、新 observation 和 timeline version。
- [x] 进入 fallback 时让所有旧 RTC worker/result 失效；恢复后只接受新 request ID、新 timeline version 和
  新 checkpoint ID。
- [x] `DelayEstimator.reset_epoch()` 后不能在没有稳定样本时直接调用 `predicted_steps()`；用恢复后新观测的
  初始推理/明确的 discarded probe 建立新 epoch 样本，禁止复用导致本次 fallback 的旧 latency。
- [x] 恢复后的初始 plan 使用 RTC checkpoint horizon `s=10`，不能继续沿用 synchronized 的 `H=20`
  checkpoint 语义。
- [x] 每个 episode 最多自动恢复 3 次；第 4 次可恢复错误只执行 timed synchronized fallback，不无限循环。
- [x] 日志增加 recovery ID、原始 failure reason、恢复阶段、旧/新 estimator epoch、stable source time、
  observation seq、plan/timeline/request ID 和最终结果。

### 离线和真机验收

- [x] fake bridge 已分别覆盖 discard deadline 的 `rtc_invalid` 和完整
  `RTC -> C2 reject -> measured hold -> sync clean -> bootstrap -> merge`；旧 request/timeline 被拒绝。
- [x] bridge 单测覆盖实测 hold、夹爪命令保持、pending RTC 清理、healthy timeout、fatal freeze hold 和 C2 原子拒绝。
- [x] timed runner 单测覆盖连续两次超时结束，以及 clean chunk 后 stuck 计数归零。
- [x] failure classifier 单测确认 C2 可恢复，clipping 和 transaction mismatch 不恢复。
- [x] 完整 fake-bridge recovery/merge 端到端注入通过，包含 session/plan/timeline/checkpoint/request ID。
- [ ] 验证新 epoch 的 `d_pred` 只来自新样本，所有成功 merge 仍满足
  `1 <= d_actual <= d_pred <= d_max`。
- [ ] 真机顺序保持：motion-disabled dry-run -> 两个 timed sync chunk -> 短 RTC recovery/merge；未通过前
  不进行长任务 soak。

## 3. 恢复夹爪实测反馈（2026-08-19，读取已启用）

- [x] 已确认控制器 `/tj/info/gripper_feedback_L/R` 可以收到左右夹爪五维信息；右侧“打开→夹持→打开”记录中，
  `q` 和 `tau` 均随阶段发生明显、可逆变化。
- [x] protocol v10 的 policy state、action state、RTC handoff anchor 和 measured hold 使用 feedback `q`。
- [ ] 实测尚未形成左右夹爪各自完整、同工况的开合端点标定，旧的 `0.0～1.25 -> 0～1` 不能视为已验证映射；
  本轮按现场决定不继续修改夹爪实现，只把它保留为任务效果评价中的独立风险。
- [x] bridge 不再用最后发布命令覆盖夹爪实测状态；任一侧 feedback 缺失或超过 state stale 阈值会关闭运动门。
- [x] telemetry 恢复原始/归一化位置、速度、力矩、温度、命令和位置误差列。
- [x] 历史 motion-disabled dry-run 已确认 protocol v9、`gripper_state_source=measured_feedback`、左右 feedback age
  `1.197/1.061 ms` 和远程 H20 policy 的 19 次成功推理；本轮未发送 action。

## 4. 2026-08-19 H20 真机结果和当前问题

### 已通过的安全与链路项目

- [x] controller doctor 读到真实 `/tj/joint_states`、状态 topic、H264 相机和两侧夹爪反馈；protocol v9
  motion-disabled dry-run 通过。
- [x] H20 单 chunk tracking 完成，操作员确认无抽动、回弹、撞击或失控；日志：
  `logs/h20_baseline_20260819_140558/tracking.log`。
- [x] synchronized 连续 3 个 H20 chunk 完成，3 次末端 tracking 均到位，`1406` 个 action tick 中 arm clipping
  为 `0`；日志：`logs/h20_baseline_20260819_141443/synchronized.log`。
- [x] continuous shadow 按预期在 `d_actual=2` 丢弃结果并进入 latched fallback，没有执行未经验证的 merge；
  日志：`logs/h20_rtc_shadow_20260819_141950/rtc-shadow.log`。
- [x] 单次真实 RTC merge 成功：`d_pred=2`、`d_actual=2`、hard-anchor `reference_delta=0.000150 rad`，使用
  3-knot quintic C2 blend，最大 jerk `15.98494 rad/s^3 < 20 rad/s^3`，无 arm clipping，操作员确认无异常；
  日志：`logs/h20_rtc_merge_20260819_142215/rtc-merge1.log`。

### 20-merge 长跑实际暴露的问题

长跑目录为 `logs/h20_rtc_soak20_20260819_142740`。操作员先后启动了两个 B 终端 episode；第二个在中途
主动退出。两次都没有成功 RTC merge，因此本轮不能用来断言“H20 连续 RTC 比 H10 差”，只能说明这两个
H20 边界没有通过 C2 安全门，后续动作实际由 synchronized fallback 执行。

- [ ] 两个 episode 的首次 RTC 结果均为 `d_pred=2`、`d_actual=2`，hard anchor 偏差也很小，但 3-knot C2
  最大 jerk 分别为 `22.06194` 和 `24.56892 rad/s^3`，超过 `20 rad/s^3` 上限；2-knot 候选更差，分别为
  `73.90878` 和 `63.91702 rad/s^3`。bridge 正确地原子拒绝 merge，并未把不安全边界发给机器人。
- [ ] 当前根因指向“旧轨迹在 merge 点的速度/加速度，与 H20 新轨迹开头不够相容”，不是网络延迟尖峰：两次
  推理均在正常量级，`d_pred=d_actual=2`，没有 stale、ID mismatch、arm clipping 或延迟越界证据。先离线回放
  这两组边界，定位贡献最大的关节，并评估更长但仍受约束的 C2 blend 是否可行；不得直接放宽 jerk 上限。
- [x] protocol v10 已解决“一次 C2 拒绝永久关闭 RTC”：原 merge 仍原子拒绝且不放宽 jerk；实测 hold 后必须
  完成一个 clean synchronized chunk，换新边界并重建 estimator epoch，才允许重新 bootstrap RTC。
- [ ] 第一段 fallback 的最后一个 H20 chunk 在目标发出后始终没有满足 `0.01 rad` 且连续稳定 `0.20 s`，
  `Joint4_L` 最大误差约 `0.01155 rad`，等待 `4 s` 轨迹时长加 `5 s` tracking timeout 后报
  `timed out waiting for trajectory event`。结合现场“机械臂已经触达桌面”，高度疑似接触约束使目标物理不可达；
  日志本身只能证明未到位，不能单独证明碰撞。不要通过取消 timeout 或放宽 tolerance 掩盖该问题。
- [x] protocol v10 的 `chunk_timed_out` 事件和日志已输出 deadline、elapsed、phase、最差关节和末端误差；
  controller 在同一锁内切换到实测 hold，不再继续追旧不可达 reference。
- [x] `--episode-seconds 60` 是整轮运动的外层安全时限，`--max-rtc-merges 20` 只是“最多允许 20 次成功
  replacement”，不是要求执行 20 个 chunk；本轮实际成功 merge 为 `0`。两者都不应直接取消。完整任务应使用
  足够但有限的 episode 时长，并把“任务时限到达”和“某个目标在 tracking timeout 内不可达”分开报告。
- [ ] 两次 B 终端复用了同一个 `rtc-soak20.log` 和 telemetry 文件：结构化日志追加了两个 episode，但 CSV 被
  第二次运行覆盖，因此第一段高频 telemetry 已丢失。以后每个 episode 必须使用新的 `RUN_DIR`/文件名；客户端
  也应考虑拒绝覆盖已有 telemetry。
- [ ] 两次 episode 的初始机器人/夹爪状态不同，第二次左夹爪反馈约为 `0.708`，且预测夹爪值频繁落到投影
  边界。它不会触发 arm C2 拒绝，但会改变 policy 输入和任务表现。H10/H20 对比必须固定机械臂初始姿态、物体
  布局和两侧夹爪状态，并把 gripper clipping 单独统计。
- [x] 第二个 episode 是操作员主动 `Ctrl+C`，bridge 随后进入 `trajectory_stopped`；这不是推理服务崩溃。

### 下一轮优先顺序

- [ ] 先离线提取成功 merge 与两次失败 merge 的旧/新边界，按关节对比位置、速度、加速度和 jerk；保留
  hard anchor、C2 和 `20 rad/s^3` 安全上限。
- [ ] 每次使用独立 RUN_DIR，再用相同初始姿态/物体/夹爪条件分别跑 H10 与 H20；
  成功 RTC merge 和 fallback episode 必须分开统计，不能混成任务成功率。
- [ ] 在没有复现至少 2 次连续安全 merge 前，不再直接做 20-merge soak；按单 merge -> 2 merges -> 10 merges
  逐级放大，并保证机械臂不会以桌面接触作为停止条件。
- [x] 第 2 节 fake-bridge recovery/merge 端到端注入已完成。
- [ ] 按 `ROBOT_RTC_TESTS.md` 最小三组顺序上真机，不直接进行长任务 soak。

## 完成标准

- [x] 当前基线的本地 deploy/OpenPI 单元测试和相关 Ruff 通过。
- [x] H20 policy metadata、请求 schedule 和实际 mask 公式一致；远程服务 checkpoint/commit/CLI/GPU 已记录。
- [x] soft mask 维持 `exp`，本轮不做 schedule 变更。
- [x] fallback 自动恢复只覆盖明确的可恢复故障，任何安全故障都不会因为恢复逻辑被降级处理。
- [x] 本 TODO 已记录 soft-mask 决策、非真机测试、H20 真机通过项和 20-merge 长跑暴露的问题。
- [ ] 将本节 H20 真机结果同步到 `ROBOT_RTC_TESTS.md` 的测试记录，保留原始日志路径和操作员现场结论。
- [ ] protocol v10 真机验收后更新 `To_Be_Optimized.md`，记录最终状态机、测试结果和剩余风险。
